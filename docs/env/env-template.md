# Moved → [`.env.example`](../../.env.example)

This file is preserved as a redirect stub. The canonical environment
template for AudioJones.com lives at the repository root:

- **[`.env.example`](../../.env.example)** — variable shape.
- **`packages/config/env.schema.ts`** — validation.
- **[`docs/DEPLOYMENT.md`](../DEPLOYMENT.md)** — provider notes and
  long-secret upload procedure.

Update those files going forward; do not edit this stub.
