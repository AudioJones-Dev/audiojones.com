# Moved → ../.env.example
#
# This file is preserved as a redirect stub. The canonical environment
# template for AudioJones.com lives at the repository root:
#
#     ../.env.example
#
# Validation lives in packages/config/env.schema.ts. Provider-specific
# notes are in docs/DEPLOYMENT.md.
#
# Do not maintain this file as a parallel template.
