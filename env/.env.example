# Superseded env template

This file is retained only as a compatibility pointer for older references.

The canonical environment-variable template is now:

- `../.env.example`

Use the canonical docs for setup and secret handling:

- `../docs/DEPLOYMENT.md`
- `../docs/SECURITY.md`

Do not add environment-variable definitions here. Update the root `.env.example` instead.
